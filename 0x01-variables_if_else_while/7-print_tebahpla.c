#include <stdio.h>

/**
 * main - print the alphabets in reverse
 * Return: Always 0
 */
int main(void)
{
	int numb;

	for (numb = 9; numb >= 0; numb--)

		putchar(numb + '0');
	}
	putchar('/n');
	return (0);
}
